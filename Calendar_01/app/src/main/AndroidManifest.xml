<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kds.activity"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name">
        <activity
            android:name="com.kds.activity.CalendarActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.kds.activity.ScheduleView"
            android:label="@string/app_name" />
        <activity
            android:name="com.kds.activity.ScheduleTypeView"
            android:label="@string/app_name" />
        <activity
            android:name="com.kds.activity.ScheduleInfoView"
            android:label="@string/app_name" />
        <activity
            android:name="com.kds.activity.ScheduleAll"
            android:label="@string/app_name" />
        <activity
            android:name="com.kds.activity.CalendarConvert"
            android:label="@string/app_name" />

    </application>
</manifest>